let repetir = true

while(repetir){
    let = numero = parseInt( prompt( " Lograr adivinar cuantos años tengo"))

    if( numero === 28){
        repetir = false
        alert ("Acertaste! buen trabajo!")
    }
    else if (isNaN (numero)){
        alert ("Solo podes ingresar numeros");

    } else {
        alert("sigue intentando") 
}
}